services:
  api:
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      Email__Provider: brevo
      Brevo__ApiKey: ${BREVO_API_KEY}
      Email__Sender: ${BREVO_EMAIL_SENDER}
    ports:
      - "8080:8080"
    restart: unless-stopped

  db:
    restart: unless-stopped
